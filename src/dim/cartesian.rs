//! Cartesian dimensions enumeration.

/// Cartesian dimension.
pub enum Cartesian {
    /// First spatial dimension.
    X = 0,
    /// Second spatial dimension.
    Y = 1,
    /// Third spatial dimension.
    Z = 2,
}
